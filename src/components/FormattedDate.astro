---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-au', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
<style>
time {
  color: var(--gray-dark);
  font-size: 1rem;
}
</style>
